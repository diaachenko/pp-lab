package Model.Cardboard;
import Model.Package.GiftContainer;

public class CardboardBox extends GiftContainer {
    public CardboardBox(String color, int weight, int volume, int load) { super(color, weight, volume, load); }
}
