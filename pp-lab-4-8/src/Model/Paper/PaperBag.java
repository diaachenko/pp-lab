package Model.Paper;

import Model.Package.GiftContainer;

public class PaperBag extends GiftContainer {
    public PaperBag(String name, int weight, int volume, int load) { super(name, weight, volume, load); }
}
