package Model.MetalBox;

import Model.Package.GiftContainer;

public class MetalBox extends GiftContainer {
    public MetalBox(String color, int weight, int volume, int load) { super(color, weight, volume, load); }
}
