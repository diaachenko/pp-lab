package Model.Glass;

import Model.Package.GiftContainer;

public class Jar extends GiftContainer {
    public Jar(String color, int weight, int volume, int load) { super(color, weight, volume, load); }
}
